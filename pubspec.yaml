name: deadlinekeeper
description: A to-do list application with notification
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^17.1.0
  timezone: ^0.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^17.1.0
  timezone: ^0.9.2
  alarm: ^2.0.5          # TAMBAHKAN INI untuk alarm
  just_audio: ^0.9.36 

flutter:
  uses-material-design: true
   assets:
    - assets/audio/